export enum PREFIX {
  mainnet = 'bitcoincash',
  chipnet = 'bchtest',
  testnet = 'bchtest',
}

export enum COIN_TYPE {
  bitcoincash = 145,
  testnet = 1,
}

export const INTERVAL = 60 * 1000; // 1-minute interval
